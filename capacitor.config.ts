import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orgasolutions.utilityapp',
  appName: 'AOUS-All in One Utility Suite',
  webDir: 'build'
};

export default config;
